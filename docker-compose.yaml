services: 
  bot: 
    build: .
    depends_on:
      - mongo
    env_file:
      - .env
    ports: 
      - "5000:5000"

  mongo: 
    image: "mongo:latest"
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes: 
  mongo_data: